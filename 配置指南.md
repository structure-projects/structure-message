# Structure Message 配置指南

## 目录
- [概述](#概述)
- [数据库表结构](#数据库表结构)
- [通道配置](#通道配置)
  - [邮件通道(EMAIL)](#邮件通道email)
  - [短信通道(SMS)](#短信通道sms)
  - [站内消息通道(INTERNAL)](#站内消息通道internal)
  - [IM消息通道(IM)](#im消息通道im)
- [配置示例](#配置示例)
- [配置管理](#配置管理)

---

## 概述

Structure Message 是一个多通道消息系统，支持邮件、短信、站内消息和多种IM渠道。所有通道配置存储在 `org_channel_config` 表中，采用JSON格式存储配置参数。

---

## 数据库表结构

### 1. 通道表 (channel)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键ID |
| channel_code | varchar(50) | 通道编码 |
| channel_name | varchar(100) | 通道名称 |
| channel_type | varchar(20) | 通道类型：INTERNAL、SMS、EMAIL、IM |
| plugin_class | varchar(200) | 插件实现类 |
| status | tinyint | 状态：1-启用，0-禁用 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |

### 2. 组织通道配置表 (org_channel_config)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键ID |
| org_id | bigint | 组织ID |
| channel_id | bigint | 通道ID |
| config_name | varchar(100) | 配置名称（用于区分同一通道的多个配置） |
| config_value | text | 配置值(JSON格式) |
| status | tinyint | 状态：1-启用，0-禁用 |
| is_default | int(1) | 是否默认配置：1-是，0-否 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |

---

## 通道配置

### 邮件通道(EMAIL)

#### 配置参数

| 参数 | 类型 | 必填 | 说明 | 示例 |
|------|------|------|------|------|
| host | String | 是 | SMTP服务器地址 | smtp.qq.com |
| port | String | 是 | SMTP端口 | 465 |
| username | String | 是 | 邮箱账号 | example@qq.com |
| password | String | 是 | 邮箱密码/授权码 | ******** |
| auth | String | 否 | 是否需要认证 | true |
| starttls | String | 否 | 是否启用STARTTLS | false |
| ssl | String | 否 | 是否启用SSL | true |
| connectiontimeout | String | 否 | 连接超时(ms | 5000 |
| timeout | String | 否 | 读取超时(ms) | 5000 |
| writetimeout | String | 否 | 写入超时(ms) | 10000 |
| from | String | 否 | 发件人邮箱 | example@qq.com |

#### 配置示例

```json
{
  "host": "smtp.qq.com",
  "port": "465",
  "username": "yunjida-mail@qq.com",
  "password": "ursqzgylxxijchjb",
  "auth": "true",
  "starttls": "false",
  "ssl": "true",
  "connectiontimeout": "5000",
  "timeout": "5000",
  "writetimeout": "10000",
  "from": "yunjida-mail@qq.com"
}
```

#### SQL插入示例

```sql
-- 插入邮件通道配置
INSERT INTO org_channel_config (org_id, channel_id, config_name, config_value, status, is_default)
VALUES (1, 1, 'QQ邮箱配置', '{
  "host": "smtp.qq.com",
  "port": "465",
  "username": "yunjida-mail@qq.com",
  "password": "ursqzgylxxijchjb",
  "auth": "true",
  "starttls": "false",
  "ssl": "true",
  "connectiontimeout": "5000",
  "timeout": "5000",
  "writetimeout": "10000",
  "from": "yunjida-mail@qq.com"
}', 1, 1);
```

---

### 短信通道(SMS)

#### 支持的提供商
- aliyun (阿里云)
- tencent (腾讯云)

#### 阿里云配置参数

| 参数 | 类型 | 必填 | 说明 | 示例 |
|------|------|------|------|------|
| provider | String | 是 | 提供商代码 | aliyun |
| accessKeyId | String | 是 | 访问密钥ID | LTAI5t7... |
| accessKeySecret | String | 是 | 访问密钥Secret | ******** |
| signName | String | 是 | 短信签名 | 测试签名 |
| region | String | 是 | 区域 | cn-hangzhou |
| domain | String | 是 | API域名 | dysmsapi.aliyuncs.com |

#### 腾讯云配置参数

| 参数 | 类型 | 必填 | 说明 | 示例 |
|------|------|------|------|------|
| provider | String | 是 | 提供商代码 | tencent |
| secretId | String | 是 | 密钥ID | AKID... |
| secretKey | String | 是 | 密钥Key | ******** |
| signName | String | 是 | 短信签名 | 测试签名 |
| region | String | 是 | 区域 | ap-guangzhou |
| appId | String | 是 | 应用ID | 1400... |

#### 配置示例 - 阿里云

```json
{
  "provider": "aliyun",
  "accessKeyId": "test-access-key-id",
  "accessKeySecret": "test-access-key-secret",
  "signName": "测试签名",
  "region": "cn-hangzhou",
  "domain": "dysmsapi.aliyuncs.com"
}
```

#### SQL插入示例

```sql
-- 插入短信通道配置
INSERT INTO org_channel_config (org_id, channel_id, config_name, config_value, status, is_default)
VALUES (1, 2, '阿里云短信配置', '{
  "provider": "aliyun",
  "accessKeyId": "test-access-key-id",
  "accessKeySecret": "test-access-key-secret",
  "signName": "测试签名",
  "region": "cn-hangzhou",
  "domain": "dysmsapi.aliyuncs.com"
}', 1, 1);
```

---

### 站内消息通道(INTERNAL)

站内消息通道无需额外配置，直接使用数据库存储消息即可。

#### 可选配置参数 (application.yml)

```yaml
message:
  plugin:
    internal:
      enabled: true
      max-storage-days: 30
      max-messages-per-user: 1000
      websocket-enabled: true
      websocket-endpoint: /ws/internal-message
      max-content-length: 2000
      max-title-length: 100
      deduplication-enabled: true
      deduplication-window-minutes: 5
```

#### SQL插入示例

```sql
-- 站内消息通道通常不需要配置，直接使用即可
-- 如需要特定配置可按以下格式插入
INSERT INTO org_channel_config (org_id, channel_id, config_name, config_value, status, is_default)
VALUES (1, 3, '默认站内消息配置', '{}', 1, 1);
```

---

### IM消息通道(IM)

#### 支持的提供商

| 提供商 | Provider | 点对点(P2P) | 自定义机器人 | 应用机器人群聊 |
|--------|----------|-----------|-------------|---------------|
| 飞书 | feishu | ✅ | ✅ | ✅ |
| 钉钉 | dingtalk | ✅ | ✅ | ❌ |
| 企业微信 | wechatwork | ✅ | ✅ | ❌ |
| 网易云信 | netease | ✅ | ❌ | ❌ |

#### 飞书配置参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| provider | String | 是 | 提供商代码 |
| appId | String | 是 | 飞书应用AppID |
| appSecret | String | 是 | 飞书应用AppSecret |
| webhookUrl | String | 否 | 飞书群机器人Webhook地址 |
| signSecret | String | 否 | 签名密钥 |
| botId | String | 否 | 机器人ID |

#### 钉钉配置参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| provider | String | 是 | 提供商代码 |
| appId | String | 是 | 钉钉应用AppKey |
| appSecret | String | 是 | 钉钉应用AppSecret |
| webhookUrl | String | 否 | 钉钉群机器人Webhook地址 |
| signSecret | String | 否 | 签名密钥 |
| botId | String | 否 | 机器人AgentId |

#### 企业微信配置参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| provider | String | 是 | 提供商代码 |
| appId | String | 是 | 企业微信corpid |
| appSecret | String | 是 | 企业微信corpsecret |
| webhookUrl | String | 否 | 企业微信群机器人Webhook地址 |
| botId | String | 否 | 应用AgentId |

#### 网易云信配置参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| provider | String | 是 | 提供商代码 |
| appId | String | 是 | 云信应用AppKey |
| appSecret | String | 是 | 云信应用AppSecret |
| botId | String | 否 | 发送方账号 |

#### 配置示例 - 飞书

```json
{
  "provider": "feishu",
  "appId": "cli_xxxxxxxxxx",
  "appSecret": "xxxxxxxxxx",
  "webhookUrl": "https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxxxx",
  "signSecret": "xxxxxxxxxx",
  "botId": "xxxxxxxxxx"
}
```

#### SQL插入示例

```sql
-- 1. 首先插入IM通道记录
INSERT INTO channel (channel_code, channel_name, channel_type, plugin_class, status)
VALUES ('IM', 'IM消息通道', 'IM', 'com.structure.message.plugin.im.ImMessagePlugin', 1);

-- 2. 插入IM通道配置
INSERT INTO org_channel_config (org_id, channel_id, config_name, config_value, status, is_default)
VALUES (1, 4, '飞书配置', '{
  "provider": "feishu",
  "appId": "cli_xxxxxxxxxx",
  "appSecret": "xxxxxxxxxx",
  "webhookUrl": "https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxxxx"
}', 1, 1);
```

---

## 配置示例

### 初始化通道数据

```sql
-- 初始化通道数据
INSERT INTO channel (channel_code, channel_name, channel_type, plugin_class, status) VALUES
('EMAIL', '企业邮件通道', 'EMAIL', 'com.structure.message.plugin.email.EmailMessagePlugin', 1),
('SMS', '短信通道', 'SMS', 'com.structure.message.plugin.sms.SmsMessagePlugin', 1),
('INTERNAL', '站内消息', 'INTERNAL', 'com.structure.message.plugin.internal.InternalMessagePlugin', 1),
('IM', 'IM消息通道', 'IM', 'com.structure.message.plugin.im.ImMessagePlugin', 1);
```

### 完整配置示例

```sql
-- 邮件通道配置
INSERT INTO org_channel_config (org_id, channel_id, config_name, config_value, status, is_default) VALUES
(1, 1, 'QQ邮箱', '{
  "host": "smtp.qq.com",
  "port": "465",
  "username": "yunjida-mail@qq.com",
  "password": "ursqzgylxxijchjb",
  "auth": "true",
  "starttls": "false",
  "ssl": "true",
  "connectiontimeout": "5000",
  "timeout": "5000",
  "writetimeout": "10000",
  "from": "yunjida-mail@qq.com"
}', 1, 1);

-- 短信通道配置
INSERT INTO org_channel_config (org_id, channel_id, config_name, config_value, status, is_default) VALUES
(1, 2, '阿里云短信', '{
  "provider": "aliyun",
  "accessKeyId": "test-access-key-id",
  "accessKeySecret": "test-access-key-secret",
  "signName": "测试签名",
  "region": "cn-hangzhou",
  "domain": "dysmsapi.aliyuncs.com"
}', 1, 1);

-- 站内消息配置
INSERT INTO org_channel_config (org_id, channel_id, config_name, config_value, status, is_default) VALUES
(1, 3, '默认配置', '{}', 1, 1);

-- IM通道配置
INSERT INTO org_channel_config (org_id, channel_id, config_name, config_value, status, is_default) VALUES
(1, 4, '飞书', '{
  "provider": "feishu",
  "appId": "cli_xxxxxxxxxx",
  "appSecret": "xxxxxxxxxx",
  "webhookUrl": "https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxxxx"
}', 1, 1);
```

---

## 配置管理

### 配置状态管理

#### 设置默认配置

```sql
-- 将指定配置设为默认配置
UPDATE org_channel_config 
SET is_default = 1 
WHERE id = 1;

-- 清除同通道其他配置的默认标记
UPDATE org_channel_config 
SET is_default = 0 
WHERE org_id = 1 
  AND channel_id = 1 
  AND id != 1;
```

#### 启用/禁用配置

```sql
-- 启用配置
UPDATE org_channel_config SET status = 1 WHERE id = 1;

-- 禁用配置
UPDATE org_channel_config SET status = 0 WHERE id = 1;
```

#### 查询配置

```sql
-- 查询组织所有启用的配置
SELECT * FROM org_channel_config 
WHERE org_id = 1 AND status = 1;

-- 查询指定通道的配置
SELECT * FROM org_channel_config 
WHERE org_id = 1 AND channel_id = 1 AND status = 1;

-- 查询默认配置
SELECT * FROM org_channel_config 
WHERE org_id = 1 AND channel_id = 1 AND is_default = 1 AND status = 1;
```

---

## 注意事项

1. **凭证安全**：生产环境中请勿将凭证硬编码在代码中，应使用环境变量或配置中心
2. **配置加密**：敏感配置信息（如密码、密钥）应加密存储
3. **配置验证**：配置变更后应验证通道是否正常工作
4. **默认配置**：每个通道应设置一个默认配置
5. **配置名称**：配置名称在同一组织同一通道下必须唯一
6. **JSON格式**：config_value必须是有效的JSON格式
