spring:
  application:
    name: structure-message-interfaces-test
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  sql:
    init:
      mode: always

server:
  port: 0

logging:
  level:
    org.springframework: WARN
    cn.structured: DEBUG